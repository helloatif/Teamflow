{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Request Rate by Route",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(teamflow_http_requests_total[5m])) by (route)",
          "legendFormat": "{{route}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Average Request Duration by Route",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(teamflow_http_request_duration_seconds_sum[5m])) by (route) / sum(rate(teamflow_http_request_duration_seconds_count[5m])) by (route)",
          "legendFormat": "{{route}}",
          "refId": "A"
        }
      ]
    }
  ],
  "schemaVersion": 41,
  "tags": ["teamflow", "http"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "timezone": "browser",
  "title": "TeamFlow HTTP Overview",
  "uid": "teamflow-http-overview",
  "version": 1
}
